{
  "name": "agent-broadcast-mcp",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["server"],
  "scripts": {
    "test": "tsx --test tests/**/*.test.ts tests/**/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.5",
    "typescript": "^5.6.0"
  }
}
